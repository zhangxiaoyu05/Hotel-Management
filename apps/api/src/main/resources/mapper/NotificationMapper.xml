<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.repository.NotificationRepository">

    <select id="findByUserId" resultType="com.hotel.entity.Notification">
        SELECT * FROM notifications
        WHERE user_id = #{userId}
        AND deleted = 0
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM notifications
        WHERE user_id = #{userId}
        AND deleted = 0
    </select>

    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM notifications
        WHERE user_id = #{userId}
        AND is_read = false
        AND deleted = 0
    </select>

    <select id="findUnreadByUserId" resultType="com.hotel.entity.Notification">
        SELECT * FROM notifications
        WHERE user_id = #{userId}
        AND is_read = false
        AND deleted = 0
        ORDER BY created_at DESC
    </select>

</mapper>