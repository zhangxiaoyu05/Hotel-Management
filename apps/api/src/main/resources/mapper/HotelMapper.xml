<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.repository.HotelRepository">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.hotel.entity.Hotel">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="facilities" property="facilities" jdbcType="LONGVARCHAR"/>
        <result column="images" property="images" jdbcType="LONGVARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="created_by" property="createdBy" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 分页查询酒店列表 -->
    <select id="selectHotelsWithPage" resultMap="BaseResultMap">
        SELECT *
        FROM hotels
        <where>
            deleted = 0
            <if test="search != null and search != ''">
                AND (name LIKE CONCAT('%', #{search}, '%')
                OR address LIKE CONCAT('%', #{search}, '%')
                OR description LIKE CONCAT('%', #{search}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        <choose>
            <when test="sortBy != null and sortBy != '' and sortDir != null and sortDir != ''">
                ORDER BY ${sortBy} ${sortDir}
            </when>
            <otherwise>
                ORDER BY created_at DESC
            </otherwise>
        </choose>
    </select>

    <!-- 根据名称查找酒店 -->
    <select id="selectByName" resultMap="BaseResultMap">
        SELECT *
        FROM hotels
        WHERE name = #{name}
        AND deleted = 0
        LIMIT 1
    </select>

</mapper>